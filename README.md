تمام 👍 هذا مثال **ملف README.md** بسيط وواضح يشرح مشروع الآلة الحاسبة والاختبارات:

---

# Calculator Project

مشروع آلة حاسبة بسيط بلغة **JavaScript (Node.js)** مع اختبارات باستخدام **Jest**.

## ✨ الميزات

* الجمع (Addition)
* الطرح (Subtraction)
* الضرب (Multiplication)
* القسمة (Division)
* معالجة القسمة على صفر (ترمي خطأ)

---

## 📁 هيكل المشروع

```
.
├── calculator.js
├── calculator.test.js
├── package.json
└── README.md
```

---

## ⚙️ المتطلبات

* Node.js (إصدار 14 أو أحدث)
* npm

---

## 📦 التثبيت

1. انسخ المشروع:

```bash
git clone <repository-url>
```

2. ادخل إلى مجلد المشروع:

```bash
cd calculator
```

3. ثبّت الاعتمادات:

```bash
npm install
```

---

## ▶️ تشغيل الاختبارات

لتشغيل جميع الاختبارات:

```bash
npm test
```

---

## 🧪 أمثلة على الاختبارات

الاختبارات تتحقق من:

* صحة العمليات الحسابية
* رمي خطأ عند القسمة على صفر

مثال:

```js
test('division', () => {
    expect(calculator.divide(10, 2)).toBe(5);
});

test('divide by zero', () => {
    expect(() => {
        calculator.divide(5, 0);
    }).toThrow(Error);
});
```

---

## 🛠️ مثال على الاستخدام

```js
const calculator = require('./calculator');

calculator.add(2, 3);        // 5
calculator.subtract(5, 3);  // 2
calculator.multiply(3, 5);  // 15
calculator.divide(10, 2);   // 5
```

---

## 📄 الترخيص

هذا المشروع مفتوح المصدر ويمكن استخدامه لأغراض تعليمية.

---

إذا حاب، أقدر أكتب لك أيضًا:

* ملف `calculator.js`
* أو نسخة README أقصر
* أو توثيق بالإنجليزي 🌍
